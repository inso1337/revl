def _rust_str_literal(s: str) -> str:\n    max_hashes = max((s.count("""), s.count('#')))\n    fence = "#" * (max_hashes + 1)\n    if f'"{fence}' in s or f'{fence}"' in s:\n        raise ValueError("String contains an unresolvable sequence of quotes and hashes.")\n    return f'r{fence}"{s}"{fence}'
