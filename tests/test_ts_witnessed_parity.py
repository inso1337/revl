"""Item 369: py<->ts parity for the WITNESSED stdlib catalogs (stdlib/fs.rvl,
stdlib/shell.rvl). Proves the @ts witnessed bodies execute AND revert on the ts
tier exactly as the @py bodies do on py.

Placeholder failing assertion committed first for resilience (this environment
SIGTERMs the host). Filled in as the @ts bodies land."""


def test_ts_witnessed_parity_placeholder():
    # TODO(369): the ts flagship proof — write/overwrite/delete then ABORT ->
    # workspace hash identical to before (residue-free) — on the ts tier.
    assert False, "item 369 @ts witnessed bodies not yet implemented"
