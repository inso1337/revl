// Generated by revl backends/typescript/emit.py — do not edit.
// Target runtime: cordis v4 (https://github.com/cordiverse/cordis).
import type { Context } from 'cordis'
import { host } from '../../runtime.ts'

const REVL_I64_MIN = -(2n ** 63n)
const REVL_I64_MAX = 2n ** 63n - 1n
function revlI64(v: bigint): bigint {
  if (v < REVL_I64_MIN || v > REVL_I64_MAX) throw new RangeError('revl: Int overflow')
  return v
}

export interface Pt {
  x: bigint
  y: bigint
}

export function built(n: bigint): bigint[] {
    let xs = []
    let i = 0n
    while ((i < n)) {
      xs.push(i)
      i = revlI64(i + 1n)
    }
    return xs
}

export function counted(keys: string[]): Map<string, bigint> {
    let m = new Map()
    for (const k of keys) {
      m.set(k, 1n)
    }
    return m
}

export function dropped(keys: string[], gone: string): Map<string, bigint> {
    let m = new Map()
    for (const k of keys) {
      m.set(k, 1n)
    }
    m.delete(gone)
    return m
}

export function moved(n: bigint): Pt {
    let p = {x: 1n, y: 2n}
    let i = 0n
    while ((i < n)) {
      Object.assign(p, { x: p.y, y: p.x })
      i = revlI64(i + 1n)
    }
    return p
}

export function snapshots(n: bigint): bigint[][] {
    let xs = [0n]
    let out = []
    let i = 1n
    while ((i < n)) {
      out.push(xs)
      xs = [...xs, i]
      i = revlI64(i + 1n)
    }
    return out
}

export function chunked(n: bigint): bigint[][] {
    let out = []
    let i = 0n
    while ((i < n)) {
      let res = []
      let j = 0n
      while ((j < 2n)) {
        res.push(revlI64(revlI64(i * 2n) + j))
        j = revlI64(j + 1n)
      }
      out.push(res)
      i = revlI64(i + 1n)
    }
    return out
}

export function appended(xs: bigint[], v: bigint): bigint[] {
    let ys = xs
    ys = [...ys, v]
    return ys
}
