;; Generated by the revl cordis-wasm backend (ir_version 3) — do not edit.
;; pure functions + documented type layouts; Int is i64 (values), addresses
;; are i32; Str/List/record values use canonical-ABI-shaped linear memory
;; (u32 length/count prefix, then one 8-byte slot per field/element)
(module
  (memory (export "memory") 1)
  (data (i32.const 0) "\0b\00\00\00nonpositive")
  (global $__hp (mut i32) (i32.const 32))
  ;; item 432(e): the per-call arena floor. Every canonical export
  ;; rewinds $__hp here on the way out, so nothing a call allocates
  ;; survives it (see `_arena_safe` for why that is sound here).
  (global $__canon_arena_base i32 (i32.const 32))
  ;; item 432(f): the canonical return area is constant for the
  ;; module (the host reads it before it can re-enter), so one
  ;; cell serves every call instead of one bump allocation each.
  (global $__canon_ret_area i32 (i32.const 16))
  (func $__heap_exhausted
    (unreachable))
  (func $__heap_grow (param $end i32)
    (local $want i32)
    (local $have i32)
    (local.set $want
      (i32.add
        (i32.shr_u (local.get $end) (i32.const 16))
        (i32.ne
          (i32.and (local.get $end) (i32.const 65535))
          (i32.const 0))))
    (local.set $have (memory.size))
    (if (i32.le_u (local.get $want) (local.get $have))
      (then (return)))
    (if (i32.eq
          (memory.grow (i32.sub (local.get $want) (local.get $have)))
          (i32.const -1))
      (then (call $__heap_exhausted))))
  (func $alloc (param $n i32) (result i32)
    (local $p i32)
    (local $sz i32)
    (local $end i32)
    (local.set $sz
      (i32.and
        (i32.add (local.get $n) (i32.const 7))
        (i32.const -8)))
    (if (i32.lt_u (local.get $sz) (local.get $n))
      (then (call $__heap_exhausted)))
    (local.set $p (global.get $__hp))
    (local.set $end (i32.add (local.get $p) (local.get $sz)))
    (if (i32.lt_u (local.get $end) (local.get $p))
      (then (call $__heap_exhausted)))
    (if (i32.gt_u
          (local.get $end)
          (i32.shl (memory.size) (i32.const 16)))
      (then (call $__heap_grow (local.get $end))))
    (global.set $__hp (local.get $end))
    (local.get $p))
  (func $alloc_str (param $len i32) (result i32)
    (local $p i32)
    (local.set $p (call $alloc (i32.add (local.get $len) (i32.const 4))))
    (i32.store (local.get $p) (local.get $len))
    (local.get $p))
  (func $int_add (param $a i64) (param $b i64) (result i64)
    (local $r i64)
    (local.set $r (i64.add (local.get $a) (local.get $b)))
    (if (i64.lt_s
          (i64.and (i64.xor (local.get $a) (local.get $r))
                   (i64.xor (local.get $b) (local.get $r)))
          (i64.const 0))
      (then unreachable))
    (local.get $r))
  ;; --- record/variant layouts (docs/syntax-2.0.md §2) ---
  ;; @record Person { name:Str age:Int }

  (func $make (export "make") (param $p_nm i32) (param $p_a i64) (result i32) (local $__revl_tmp i32)
    (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i64.store (local.get $__revl_tmp) (i64.extend_i32_u (local.get $p_nm)))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (local.get $p_a))
      (local.get $__revl_tmp)
    return)

  (func $age_of (export "age_of") (param $p_p i32) (result i64) (local $__revl_tmp i32)
    (i64.load (i32.add (local.get $p_p) (i32.const 8)))
    return)

  (func $rename (export "rename") (param $p_p i32) (param $p_nm i32) (result i32) (local $__revl_tmp i32)
    (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i64.store (local.get $__revl_tmp) (i64.extend_i32_u (local.get $p_nm)))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (i64.load (i32.add (local.get $p_p) (i32.const 8))))
      (local.get $__revl_tmp)
    return)

  (func $dbl (export "dbl") (param $p_n i64) (result i64) (local $__revl_tmp i32)
    (local.get $p_n)
      (local.get $p_n)
      (call $int_add)
    return)

  (func $flip (export "flip") (param $p_b i32) (result i32) (local $__revl_tmp i32)
    (local.get $p_b)
      (i32.eqz)
    return)

  (func $pair (export "pair") (param $p_a i64) (param $p_b i64) (result i32) (local $__revl_tmp i32)
    (call $alloc (i32.const 24))
      (local.set $__revl_tmp)
      (i32.store (local.get $__revl_tmp) (i32.const 2))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (local.get $p_a))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 16)) (local.get $p_b))
      (local.get $__revl_tmp)
    return)

  (func $head (export "head") (param $p_xs i32) (result i64) (local $__revl_tmp i32)
    (i64.load (i32.add (local.get $p_xs) (i32.const 8)))
    return)

  (func $roster (export "roster") (param $p_nm i32) (param $p_a i64) (result i32) (local $__revl_tmp i32) (local $__revl_tmp_n1 i32)
    (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i32.store (local.get $__revl_tmp) (i32.const 1))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (i64.extend_i32_u (call $alloc (i32.const 16))
      (local.set $__revl_tmp_n1)
      (i64.store (local.get $__revl_tmp_n1) (i64.extend_i32_u (local.get $p_nm)))
      (i64.store (i32.add (local.get $__revl_tmp_n1) (i32.const 8)) (local.get $p_a))
      (local.get $__revl_tmp_n1)))
      (local.get $__revl_tmp)
    return)

  (func $maybe (export "maybe") (param $p_n i64) (result i32) (local $__revl_tmp i32)
    (local.get $p_n)
      (i64.const 0)
      (i64.gt_s)
      (if (result i32)
        (then (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i32.store (local.get $__revl_tmp) (i32.const 1))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (local.get $p_n))
      (local.get $__revl_tmp))
        (else (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i32.store (local.get $__revl_tmp) (i32.const 0))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (i64.const 0))
      (local.get $__revl_tmp)))
    return)

  (func $or_zero (export "or_zero") (param $p_o i32) (result i64) (local $__revl_tmp i32)
    (local.get $p_o)
      (local.set $__revl_tmp)
      (if (result i64)
        (i32.eq (i32.load (local.get $__revl_tmp)) (i32.const 1))
        (then (i64.load (i32.add (local.get $__revl_tmp) (i32.const 8))))
        (else (i64.const 0)))
    return)

  (func $checked (export "checked") (param $p_n i64) (result i32) (local $__revl_tmp i32)
    (local.get $p_n)
      (i64.const 0)
      (i64.gt_s)
      (if (result i32)
        (then (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i32.store (local.get $__revl_tmp) (i32.const 0))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (local.get $p_n))
      (local.get $__revl_tmp))
        (else (call $alloc (i32.const 16))
      (local.set $__revl_tmp)
      (i32.store (local.get $__revl_tmp) (i32.const 1))
      (i64.store (i32.add (local.get $__revl_tmp) (i32.const 8)) (i64.extend_i32_u (i32.const 0)))
      (local.get $__revl_tmp)))
    return)
  ;; --- canonical ABI boundary (item 41) ---
  ;; cabi_realloc: the standard allocator a component host calls to
  ;; place an incoming string/list into this module's memory. Backed
  ;; by the same bump heap ($__hp / $alloc); old/align are unused (a
  ;; bump allocator never frees, and $alloc already 8-aligns).
  ;; item 432(a): every buffer is handed out with 8 bytes of headroom
  ;; below it, so lifting a canonical string into the internal
  ;; [u32 len][bytes] layout is one store instead of a copy.
  (func (export "cabi_realloc")
      (param $old i32) (param $old_size i32) (param $align i32) (param $new_size i32)
      (result i32)
    (i32.add
      (call $alloc (i32.add (local.get $new_size) (i32.const 8)))
      (i32.const 8)))
  ;; bare (ptr,len) canonical string <-> internal [u32 len][bytes].
  ;; The lift is zero-copy for a buffer that came from the
  ;; cabi_realloc above (every conforming host's string param), and
  ;; falls back to a bulk copy for any pointer outside this heap.
  (func $__canon_lift_str (param $ptr i32) (param $len i32) (result i32)
    (local $s i32)
    (if (i32.ge_u (local.get $ptr) (i32.const 40))
      (then
        (i32.store (i32.sub (local.get $ptr) (i32.const 4)) (local.get $len))
        (return (i32.sub (local.get $ptr) (i32.const 4)))))
    (local.set $s (call $alloc_str (local.get $len)))
    (memory.copy
      (i32.add (local.get $s) (i32.const 4))
      (local.get $ptr)
      (local.get $len))
    (local.get $s))
  (func $__canon_lower_str (param $s i32) (param $dst i32)
    (i32.store (local.get $dst) (i32.add (local.get $s) (i32.const 4)))
    (i32.store offset=4 (local.get $dst) (i32.load (local.get $s))))
  (func $__canon_lower_rec_Person (param $r i32) (param $c i32)
    (call $__canon_lower_str (i32.wrap_i64 (i64.load (local.get $r))) (local.get $c))
    (i64.store (i32.add (local.get $c) (i32.const 8)) (i64.load (i32.add (local.get $r) (i32.const 8)))))
  ;; item 432(c): `Int` lays out identically canonical and internal, so the
  ;; internal body IS the canonical array: point at it, copy nothing.
  (func $__canon_lower_list_Int (param $r i32) (param $dst i32)
    (i32.store (local.get $dst) (i32.add (local.get $r) (i32.const 8)))
    (i32.store offset=4 (local.get $dst) (i32.load (local.get $r))))
  ;; item 432(c): `Int` lays out identically canonical and internal, so the
  ;; whole body crosses in one bulk copy, not a load/store per element.
  (func $__canon_lift_list_Int (param $p i32) (param $len i32) (result i32)
    (local $r i32)
    (local.set $r (call $alloc (i32.add (i32.const 8) (i32.mul (local.get $len) (i32.const 8)))))
    (i32.store (local.get $r) (local.get $len))
    (memory.copy (i32.add (local.get $r) (i32.const 8)) (local.get $p) (i32.mul (local.get $len) (i32.const 8)))
    (local.get $r))
  (func $__canon_lower_list_Person (param $r i32) (param $dst i32)
    (local $count i32) (local $i i32) (local $buf i32)
    (local.set $count (i32.load (local.get $r)))
    (local.set $buf (call $alloc (i32.mul (local.get $count) (i32.const 16))))
    (block (loop
      (br_if 1 (i32.ge_u (local.get $i) (local.get $count)))
      (call $__canon_lower_rec_Person (i32.wrap_i64 (i64.load (i32.add (local.get $r) (i32.add (i32.const 8) (i32.mul (local.get $i) (i32.const 8)))))) (i32.add (local.get $buf) (i32.mul (local.get $i) (i32.const 16))))
      (local.set $i (i32.add (local.get $i) (i32.const 1)))
      (br 0)))
    (i32.store (local.get $dst) (local.get $buf))
    (i32.store offset=4 (local.get $dst) (local.get $count)))
  (func $__canon_lower_var_Opt_Int_ (param $r i32) (param $c i32)
    (i32.store8 (local.get $c) (i32.load (local.get $r)))
    (if (i32.eq (i32.load (local.get $r)) (i32.const 1)) (then (i64.store (i32.add (local.get $c) (i32.const 8)) (i64.load (i32.add (local.get $r) (i32.const 8)))))))
  (func $__canon_lower_var_Result_Int__Str_ (param $r i32) (param $c i32)
    (i32.store8 (local.get $c) (i32.load (local.get $r)))
    (if (i32.eq (i32.load (local.get $r)) (i32.const 0)) (then (i64.store (i32.add (local.get $c) (i32.const 8)) (i64.load (i32.add (local.get $r) (i32.const 8))))))
    (if (i32.eq (i32.load (local.get $r)) (i32.const 1)) (then (call $__canon_lower_str (i32.wrap_i64 (i64.load (i32.add (local.get $r) (i32.const 8)))) (i32.add (local.get $c) (i32.const 8))))))
  ;; canonical export of `make` -> WIT `registry#make`
  (func (export "revl:exported/registry#make") (param $c0 i32) (param $c1 i32) (param $c2 i64) (result i32)
    (local $a0 i32) (local $a1 i64) (local $rv i32) (local $area i32)
    (local.set $a0 (call $__canon_lift_str (local.get $c0) (local.get $c1)))
    (local.set $a1 (local.get $c2))
    (local.set $rv (call $make (local.get $a0) (local.get $a1)))
    (local.set $area (global.get $__canon_ret_area))
    (call $__canon_lower_rec_Person (local.get $rv) (local.get $area))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $area))
  ;; canonical export of `age_of` -> WIT `registry#age-of`
  (func (export "revl:exported/registry#age-of") (param $c0 i32) (param $c1 i32) (param $c2 i64) (result i64)
    (local $t1 i32) (local $a0 i32) (local $rv i64)
    (local.set $t1 (call $alloc (i32.const 16)))
    (i64.store (local.get $t1) (i64.extend_i32_u (call $__canon_lift_str (local.get $c0) (local.get $c1))))
    (i64.store (i32.add (local.get $t1) (i32.const 8)) (local.get $c2))
    (local.set $a0 (local.get $t1))
    (local.set $rv (call $age_of (local.get $a0)))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $rv))
  ;; canonical export of `rename` -> WIT `registry#rename`
  (func (export "revl:exported/registry#rename") (param $c0 i32) (param $c1 i32) (param $c2 i64) (param $c3 i32) (param $c4 i32) (result i32)
    (local $t2 i32) (local $a0 i32) (local $a1 i32) (local $rv i32) (local $area i32)
    (local.set $t2 (call $alloc (i32.const 16)))
    (i64.store (local.get $t2) (i64.extend_i32_u (call $__canon_lift_str (local.get $c0) (local.get $c1))))
    (i64.store (i32.add (local.get $t2) (i32.const 8)) (local.get $c2))
    (local.set $a0 (local.get $t2))
    (local.set $a1 (call $__canon_lift_str (local.get $c3) (local.get $c4)))
    (local.set $rv (call $rename (local.get $a0) (local.get $a1)))
    (local.set $area (global.get $__canon_ret_area))
    (call $__canon_lower_rec_Person (local.get $rv) (local.get $area))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $area))
  ;; canonical export of `dbl` -> WIT `registry#dbl`
  (func (export "revl:exported/registry#dbl") (param $c0 i64) (result i64)
    (local $a0 i64) (local $rv i64)
    (local.set $a0 (local.get $c0))
    (local.set $rv (call $dbl (local.get $a0)))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $rv))
  ;; canonical export of `flip` -> WIT `registry#flip`
  (func (export "revl:exported/registry#flip") (param $c0 i32) (result i32)
    (local $a0 i32) (local $rv i32)
    (local.set $a0 (local.get $c0))
    (local.set $rv (call $flip (local.get $a0)))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $rv))
  ;; canonical export of `pair` -> WIT `registry#pair`
  (func (export "revl:exported/registry#pair") (param $c0 i64) (param $c1 i64) (result i32)
    (local $a0 i64) (local $a1 i64) (local $rv i32) (local $area i32)
    (local.set $a0 (local.get $c0))
    (local.set $a1 (local.get $c1))
    (local.set $rv (call $pair (local.get $a0) (local.get $a1)))
    (local.set $area (global.get $__canon_ret_area))
    (call $__canon_lower_list_Int (local.get $rv) (local.get $area))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $area))
  ;; canonical export of `head` -> WIT `registry#head`
  (func (export "revl:exported/registry#head") (param $c0 i32) (param $c1 i32) (result i64)
    (local $a0 i32) (local $rv i64)
    (local.set $a0 (call $__canon_lift_list_Int (local.get $c0) (local.get $c1)))
    (local.set $rv (call $head (local.get $a0)))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $rv))
  ;; canonical export of `roster` -> WIT `registry#roster`
  (func (export "revl:exported/registry#roster") (param $c0 i32) (param $c1 i32) (param $c2 i64) (result i32)
    (local $a0 i32) (local $a1 i64) (local $rv i32) (local $area i32)
    (local.set $a0 (call $__canon_lift_str (local.get $c0) (local.get $c1)))
    (local.set $a1 (local.get $c2))
    (local.set $rv (call $roster (local.get $a0) (local.get $a1)))
    (local.set $area (global.get $__canon_ret_area))
    (call $__canon_lower_list_Person (local.get $rv) (local.get $area))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $area))
  ;; canonical export of `maybe` -> WIT `registry#maybe`
  (func (export "revl:exported/registry#maybe") (param $c0 i64) (result i32)
    (local $a0 i64) (local $rv i32) (local $area i32)
    (local.set $a0 (local.get $c0))
    (local.set $rv (call $maybe (local.get $a0)))
    (local.set $area (global.get $__canon_ret_area))
    (call $__canon_lower_var_Opt_Int_ (local.get $rv) (local.get $area))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $area))
  ;; canonical export of `or_zero` -> WIT `registry#or-zero`
  (func (export "revl:exported/registry#or-zero") (param $c0 i32) (param $c1 i64) (result i64)
    (local $t3 i32) (local $a0 i32) (local $rv i64)
    (local.set $t3 (call $alloc (i32.const 16)))
    (i32.store (local.get $t3) (local.get $c0))
    (i64.store offset=8 (local.get $t3) (i64.const 0))
    (if (i32.eq (local.get $c0) (i32.const 1)) (then (i64.store (i32.add (local.get $t3) (i32.const 8)) (local.get $c1))))
    (local.set $a0 (local.get $t3))
    (local.set $rv (call $or_zero (local.get $a0)))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $rv))
  ;; canonical export of `checked` -> WIT `registry#checked`
  (func (export "revl:exported/registry#checked") (param $c0 i64) (result i32)
    (local $a0 i64) (local $rv i32) (local $area i32)
    (local.set $a0 (local.get $c0))
    (local.set $rv (call $checked (local.get $a0)))
    (local.set $area (global.get $__canon_ret_area))
    (call $__canon_lower_var_Result_Int__Str_ (local.get $rv) (local.get $area))
    (global.set $__hp (global.get $__canon_arena_base))
    (local.get $area))
)
