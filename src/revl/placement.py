import socket\nimport time\n\ndef seam_latency_ms(host: str, port: int, samples: int = 5, timeout: float = 1.0) -> float | None:\n    """Optimized: Reuse a single socket for multiple samples."""\n    try:\n        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n        sock.settimeout(timeout)\n        sock.connect((host, port))\n        rtts = []\n        for _ in range(max(1, samples)):\n            start = time.perf_counter()\n            sock.send(b'\0')  # Send a dummy byte\n            rtts.append((time.perf_counter() - start) * 1000.0)\n        sock.close()\n        rtts.sort()\n        return round(rtts[len(rtts) // 2], 3)\n    except OSError:\n        return None
