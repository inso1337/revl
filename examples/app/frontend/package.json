{
  "name": "revl-notes-console",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The exemplary revl notes app frontend (item 462): a Vite/Vue console registered through the webui coeffect, consuming the NotesApi typed routes.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@cordisjs/client": "^0.3.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vue-tsc": "^2.1.0"
  }
}
