// REJECTED — `#` is the Python/shell line-comment lead-in; revl comments are
// `//` (item 384). The `#` used to report the opaque `unexpected character
// '#'`.
//
// expected error:
//   revl has no `#` comments

fn f() -> Int {
  # a comment
  return 0
}
