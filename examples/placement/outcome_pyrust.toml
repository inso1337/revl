# Cross-language ADT/Result: Python provider (DirSvc), rust consumer.
[processes.provider]
components = ["DirSvc"]
[processes.consumer]
backend = "rust"
components = ["DirUser"]
probe = ["dir.lookup('ada')", "dir.check('ada')"]
