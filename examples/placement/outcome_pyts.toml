# Cross-language ADT/Result: Python provider (DirSvc), node consumer.
[processes.provider]
components = ["DirSvc"]
[processes.consumer]
backend = "node"
components = ["DirUser"]
probe = ["dir.lookup('ada')", "dir.check('ada')"]
