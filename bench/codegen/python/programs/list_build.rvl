// A loop that accumulates a list, the single most common shape in any
// program. `xs.push(v)` is the only spelling revl has for appending.
fn build(n: Int) -> List[Int] {
  var out: List[Int] = []
  var i = 0
  while (i < n) {
    out = out.push(i)
    i += 1
  }
  return out
}
